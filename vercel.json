{
    "version": 2,
    "installCommand": "if [ ! -d \"flutter_sdk\" ]; then git clone https://github.com/flutter/flutter.git -b stable flutter_sdk; fi && ./flutter_sdk/bin/flutter config --enable-web && ./flutter_sdk/bin/flutter precache --web",
    "buildCommand": "./flutter_sdk/bin/flutter build web --release --no-tree-shake-icons --verbose",
    "outputDirectory": "build/web",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}